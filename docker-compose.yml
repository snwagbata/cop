services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: cop
      POSTGRES_PASSWORD: cop_dev_only
      POSTGRES_DB: cop
    ports:
      - "5432:5432"
    volumes:
      - cop_pgdata:/var/lib/postgresql/data

volumes:
  cop_pgdata:
