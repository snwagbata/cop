node_modules/
dist/
build/
*.log
.env
.env.local

# Active agent worktrees (isolation: "worktree" runs) — these are separate
# git checkouts under their own branches, not content of this branch.
.claude/worktrees/
